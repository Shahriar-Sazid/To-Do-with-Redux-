import React from 'react';

const title = props => {
  return (
    <div
      style={{
        color: 'rgba(255, 0, 0, 0.1)',
        textAlign: 'center',
        fontSize: '5rem',
        margin: '0 auto',
        fontWeight: 600
      }}
    >
      todos
    </div>
  );
};

export default title;
